import styled from 'styled-components';
import mq from '../../../constants/layout';

// export const Root = styled.div<{ searching: boolean }>`
//   padding-top: 3rem;
//   padding-left: 5px;
//   padding-right: 5px;
//   padding-bottom: 5px;

//   ${mq.smUp} {
//     padding: ${(props) => (props.searching ? '0' : '0 40px 40px 40px')};
//   }
// `;
