enum zIndex {
  toast = 12,
  header = 10,
  superhero = 2,
  lowerOverlay = 1,
  upperOverlay = 2,
  aboveOverlay = 3,
  checkedCardIcon = 1,
  cardChip = 1,
  lightbox = 11,
}

export default zIndex;
